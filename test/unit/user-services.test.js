const { expect }    = require('chai');
const userServices     = require('../../src/services/users');

describe('User services :-', () => {
    it ('should be generate btc address', (done) => {
        let generateAddress = userServices.generateAddress();
        
    });
});