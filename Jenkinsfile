pipeline {
    agent any 
    stages {
        stage('Docker Build') { 
            steps {
                echo "This is Build stage."
            }
        }
        stage('Docker push') { 
            steps {
                echo "This is docker push stage." 
            }
        }
    }
}
