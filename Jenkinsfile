pipeline {
    agent any 
    stages {
        stage('Docker push') { 
            steps {
                echo "This is docker push stage."
                echo "this qa branch-pipeline"
            }
        }
    }
}
